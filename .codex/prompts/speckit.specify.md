# Specify

Create or update `specs/<feature>/spec.md` with user needs, scope, requirements, non-goals, and acceptance criteria. Keep the specification implementation-neutral except where explicit contracts are required.

